const Parser = require('rss-parser')

/* eslint-disable */

export function handler(event, context, callback) {
	//let newFeed = event.target.value;
	console.log(context);
	// let parser = new Parser();

	// const feedRequest = parser.parseURL(newFeed);

	// Promise(feedRequest).then(response => {
	// 	callback(null, {
	// 		statusCode: 200,
	// 		body: JSON.stringify(response)
	// 	});
	// });
}